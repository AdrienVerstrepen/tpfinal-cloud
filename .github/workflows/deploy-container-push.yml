name: Build and Deploy on push on main
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: docker build BUILD_ID=${{ github.sha }} -t ${{ secrets.DOCKER_USERNAME }}/vue-app:${{ github.sha }}-latest . 

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/vue-app:${{ github.sha }}-latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'TPFinal CP Adrien Verstrepen'
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          images: '${{ secrets.DOCKER_USERNAME }}/vue-app:${{ github.sha }}'